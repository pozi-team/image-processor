{
  "name": "@pozible/image-processor",
  "version": "1.1.2",
  "description": "Process images uploaded to GCS bucket.",
  "main": "index.js",
  "types": "index.d.ts",
  "repository": "git@github.com:pozible/image-processor.git",
  "author": "spondbob <spondbob@eamca.com>",
  "keywords": [
    "sharp",
    "cloud storage"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/sharp": "^0.30.2",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "@google-cloud/storage": "^5.8.5",
    "sharp": "^0.30.4"
  },
  "scripts": {
    "build": "tsc && cp README.md dist && cp package.json dist && cp package-lock.json dist"
  },
  "engines": {
    "node": ">=10"
  }
}
